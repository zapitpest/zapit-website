# Friday Weekly Status — Week 1

**To:** zaydan@zapitpestmelbourne.com.au
**Cc:** amandi@meetapex.ai, alisher@meetapex.ai
**Subject:** Zap It MVP — Weekly Status (Week 1, ending 27 June 2026)

---

Hi Adam,

Here is the first weekly status update, in the five-field format we agreed.

**Hours consumed (this week):** Approximately 13 hours
- Phase 1 — pre-work foundation (analytics module, BigQuery SQL files, GTM blueprint, Looker wireframes, docs): ~3 hrs
- Phase 1 — Netlify auto-deploy setup + end-to-end verification: ~0.5 hrs
- Phase 1 — GCP IAM access work (Domain Restricted Sharing policy fix at org level, project creation, billing link, BigQuery API enable, Editor grant): ~1.5 hrs
- Phase 1 — BigQuery bootstrap automation script (idempotent infrastructure-as-code): ~0.5 hrs
- Phase 1 — 301 redirect map implementation + production verification (50+ rules, all curl-tested): ~1.5 hrs
- Phase 1 — Build verification, sanity testing, audit work: ~0.5 hrs
- Phase 3 — Feature Parity & Cutover Readiness audit (430-URL inventory of old WordPress, gap analysis, draft document with redirect map): ~2 hrs
- Communication, status reporting, planning: ~1 hr

**Hours remaining (against agreed MVP scope of approximately 35 hours + buffer):** Approximately 22 hours

**Work completed this week:**
- Phase 0 foundation fully shipped to your GitHub repo: analytics module, BigQuery SQL bootstrap files, GTM container blueprint, Looker Studio wireframes, pre-launch checklist
- Netlify wired up for auto-deploy from the GitHub repo — every commit to `main` now publishes to https://zapitpestmelbourne.netlify.app within ~30 seconds. Verified end-to-end.
- GCP project `zapit-business-intelligence` created under your organisation, billing linked, BigQuery API enabled. Domain Restricted Sharing policy fix applied at organisation level with both Workspace Customer IDs allowlisted. My Apex account added as Editor on the project (not Owner — full ownership remains with you).
- Feature Parity & Cutover Readiness audit drafted (`docs/FEATURE_PARITY_AUDIT.md` in the repo). Covers URL inventory comparison between the current WordPress site (430 indexed URLs) and the rebuilt Next.js site (~120 URLs), gap analysis across pages/forms/calculators/tracking/SEO, and a draft 301 redirect map. This is the cutover gate document you asked for — it identifies several items needing your input before DNS cutover.
- BigQuery bootstrap automation script committed to the repo (`scripts/bootstrap-bigquery.sh`). Idempotent shell script that verifies gcloud install + authentication, sets the active project, smoke tests BigQuery, and runs the SQL files automatically. One command replaces a dozen manual console steps and makes the setup reproducible.
- Implemented 56 high-confidence 301 redirects in `public/_redirects` for the path restructuring between old WordPress URLs and the rebuilt Next.js URLs. Covers all 15 pest-service path migrations (`/pest-solutions/X` → `/X`), 11 commercial slug migrations, intentional-removal redirects (pricing, blogs, links, certificates, industrial), 3 Melbourne regional landing pages, and several typo/legacy URL corrections. All redirects verified end-to-end with curl tests on the live staging URL — each returning HTTP 301 with the correct destination, and the target pages returning 200. Redirects requiring your input (231 missing suburb pages, 59 individual blog posts) are deliberately NOT included yet — they are flagged in the audit document for your review and decision, with a thematic mapping reference file at `docs/audit-data/blog-urls-needing-decision.txt`.
- Deep systematic audit pass: ran curl tests against ALL 426 old WordPress URLs against the live staging site to confirm actual HTTP status codes for each. Findings reflected in the Feature Parity audit v2 — these are now real measured numbers, not estimates. The audit confirms what is working (pest-services 20/20 redirected or native, commercials 18/18 covered) and what still needs your decisions (suburbs and blog posts).
- Installed and configured gcloud CLI locally (Google Cloud SDK 574.0.0 with bq 2.1.33) so the BigQuery bootstrap script can run end-to-end as soon as the OAuth login step is completed.
- Ran a senior-engineering production audit across the full MVP codebase: TypeScript strict check, ESLint, bundle size analysis, structured-data verification on live HTML, sitemap and robots.txt verification. Result: all engagement-authored code is lint-clean and TypeScript-clean, the live HTML emits 10 Schema.org types (Organization, LocalBusiness, AggregateRating, ContactPoint, OpeningHoursSpecification, PostalAddress, GeoCoordinates, City, BreadcrumbList, ListItem — strong SEO foundation), the sitemap returns 116 indexed URLs, and robots.txt is correctly configured.
- Refactored two analytics components (`AnalyticsDebugOverlay` and `AnalyticsTester`) from `setState-in-effect` pattern to React's canonical `useSyncExternalStore` for reading URL query parameters. Eliminates a class of cascading-render risk and brings the analytics module to production-grade React compliance.

**Current blockers:**
- Waiting on your preferred approach for GA4 and GTM access. The existing GA4 accounts and the current GTM container (`GTM-WBZC2BHL`) are administered by an external party (likely the previous web developer or SEO agency). I cannot add my account to them. Your reply on Option A (we create fresh accounts under our login and invite you as Administrator) vs Option B (you chase the previous admin to grant access) will determine the next concrete step on the tracking stack.
- Waiting on your answers to the two Telegram Production Bot questions (separate GCP project structure + secure delivery channel for the OAuth credentials). Once those land, that small piece of work can be slotted in alongside Phase 1.
- Several items in the Feature Parity audit need your input before cutover, most importantly: how to handle the 231 suburb landing pages on the old site that are not currently on the rebuilt site, and how to handle the 68 blog posts that were removed per the Figma brief. These are SEO-significant and worth a focused conversation.

**Planned work next week:**
- The moment GA4/GTM access path is confirmed, create the fresh accounts under our login, invite you as Administrator, capture the Measurement ID and Container ID, link GA4 to BigQuery, and configure the GTM container per the blueprint document.
- Run the BigQuery dataset bootstrap (SQL files already prepared in the repo) to materialise the 12 datasets and reserved table shells. No further input from your side needed for this.
- Begin Phase 2 event configuration in GTM: 9 dataLayer variables, SHA-256 hashing for PII, 8 conversion event triggers, 13+ tags spanning GA4 and Meta Pixel.
- Share the draft Feature Parity audit with you for review and decisions on the items flagged above.
- Sketch a first redirect map proposal for the missing suburb pages and blog posts, with options for you to choose from.

Anything else you would like me to add to next week's update — let me know.

Thanks,
Sharjeel Saleem
Apex AI
